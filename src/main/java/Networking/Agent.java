package Networking;

/**
 * Represents an agent that can run tasks in a separate thread.
 * This interface extends Runnable, allowing implementations to define
 * their own run method for executing tasks.
 */
public interface Agent extends Runnable {
}
